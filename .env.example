# Pooled Neon connection used by the application at runtime.
DATABASE_URL="postgresql://user:password@pooled-host.example/dbname?sslmode=require"

# Direct Neon connection used only by Prisma CLI migrations.
DATABASE_URL_UNPOOLED="postgresql://user:password@direct-host.example/dbname?sslmode=require"
